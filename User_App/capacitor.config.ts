import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.foodies.ionic.seven.capacitor.user',
  appName: 'Foodies Full App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
